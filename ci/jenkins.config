# Jenkins Configuration for Terraform
# This file contains default parameters for Terraform deployments via Jenkins

# Default container properties
CONTAINER_NAME=terraform-docker-demo
EXTERNAL_PORT=8000
IMAGE_NAME=nginx:latest

# Terraform executable path (should match the tool name in Jenkins)
TERRAFORM_TOOL=terraform

# Default environment
DEFAULT_ENVIRONMENT=dev

# Workspace configuration
TERRAFORM_WORKSPACE_ROOT=environments
